{
  "description": "Default configuration values that can be overridden in settings. These are fallback values when no user configuration is provided.",

  "api": {
    "api_base": "https://api.deepseek.com",
    "model": "deepseek-chat"
  },

  "command_format": {
    "command_start": "YLDEXECUTE:",
    "command_separator": "￥|"
  },

  "model_parameters": {
    "temperature": 1.0,
    "max_tokens": 4096,
    "top_p": 1.0,
    "stream": true,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },

  "execution": {
    "max_iterations": 15
  },

  "mcp": {
    "mcp_paths": [],
    "enabled_mcp_tools": []
  }
}
